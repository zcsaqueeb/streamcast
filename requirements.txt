pyrogram==2.0.106
TgCrypto
motor
aiohttp>=3.9.0
Jinja2>=3.1.0
python-dotenv
# Optional: real content-sniffing fallback for files with a missing/wrong
# extension (web/app.py degrades gracefully to extension-only guessing if
# this — or the underlying libmagic system library — isn't available).
python-magic; platform_system != "Windows"
python-magic-bin; platform_system == "Windows"
